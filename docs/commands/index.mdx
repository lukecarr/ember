---
title: "Overview"
description: "Every command Ember adds to your server."
---

Ember adds a single root command, `/ember`, with subcommands for each piece of functionality exposed through chat. Every subcommand has its own page covering syntax, the [permission](/server-owners/permissions) it requires, and an example.

If you can't run a command, ask your server owner to grant the corresponding permission node.
