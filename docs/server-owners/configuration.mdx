---
title: "Configuration"
description: "Ember's configuration surface."
---

Ember has no configuration options in `v0.1.0-alpha.1`. There is no `config.yml` to manage — installing the plugin is the whole setup.

A `config.yml` will be introduced as configurable features land, and this page will grow with it.
